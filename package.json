{
	"name": "only-messages",
	"version": "1.0.0",
	"private": true,
	"engines": {
		"node": ">=20.10.0"
	},
	"description": "An exclusive clubhouse where the members can write anonymous posts.",
	"main": "index.js",
	"scripts": {
		"start": "NODE_ENV=production node --env-file=.env --env-file=.env.production index",
		"pro": "NODE_ENV=production node --env-file=.env --env-file=.env.development --watch index",
		"dev": "NODE_ENV=development node --env-file=.env --env-file=.env.development --watch index"
	},
	"dependencies": {
		"bcrypt": "^5.1.1",
		"compression": "^1.7.4",
		"connect-mongo": "^5.1.0",
		"debug": "~2.6.9",
		"express": "~4.16.1",
		"express-async-handler": "^1.2.0",
		"express-session": "^1.18.0",
		"express-validator": "^7.0.1",
		"helmet": "^7.1.0",
		"http-errors": "~1.6.3",
		"mongoose": "^8.1.2",
		"morgan": "~1.9.1",
		"passport": "^0.7.0",
		"passport-local": "^1.0.0",
		"pug": "3.0.1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@flydotio/dockerfile": "^0.5.4"
	}
}
